# Aider Configuration for EnokMethod

# Model settings
model: gpt-4
edit-format: diff

# Git integration
auto-commits: true
commit-prompt: |
    Generate a conventional commit message for these changes.
    Follow the format: type(scope): description
    
    Types: feat, fix, docs, style, refactor, test, chore
    
    Consider the CURRENT_SPEC.md if it exists.

# File management
auto-lint: true
test-cmd: npm test

# EnokMethod specific
read:
    - .enokMethod/CONTEXT.md
    - .enokMethod/MEMORY.md
    - CURRENT_SPEC.md
    - CONVENTIONS.md

# Workflow commands (EnokMethod Namespaced)
aliases:
    # Short aliases
    spec: enokmethod spec "$1"
    done: enokmethod done "$1"
    status: enokmethod status
    validate: enokmethod validate
    commit: enokmethod commit
    dev: enokmethod dev
    
    # Namespaced aliases (Explicit)
    "enokMethod:spec": enokmethod spec "$1"
    "enokMethod:done": enokmethod done "$1"
    "enokMethod:status": enokmethod status
    "enokMethod:validate": enokmethod validate
    "enokMethod:commit": enokmethod commit
    "enokMethod:dev": enokmethod dev
